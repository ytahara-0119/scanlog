[project]
name = "scanlog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ytahara-0119", email = "yoshinobu0119tahara@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "sqlalchemy>=2.0.48",
    "typer>=0.24.1",
]

[project.scripts]
scanlog = "scanlog.cli:app"

[build-system]
requires = ["uv_build>=0.10.11,<0.11.0"]
build-backend = "uv_build"
